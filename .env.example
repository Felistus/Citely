# Copy this file to .env.local and fill in real values.
# .env.local is gitignored — never commit real secrets.

# Google Gemini API key (used by the AI Answer Simulator)
GEMINI_API_KEY=

# Google PageSpeed Insights API key (used by the Core Web Vitals module)
PSI_API_KEY=
